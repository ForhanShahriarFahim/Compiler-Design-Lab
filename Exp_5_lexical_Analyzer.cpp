#include<bits/stdc++.h>
using namespace std;

void find(string s)
{
    if((s[0]>='a' && s[0]<='h') || (s[0])>='A' && s[0]<='H') || (s[0])>='o' && s[0]<='H') )
}
int main()
{
    string s;
    cin>>s;
    find(s);
}